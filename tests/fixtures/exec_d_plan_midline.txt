Posting the PLAN block. PLAN READY: B.010.circleci-three-reds-triage (EXEC-D)

1. What to run: exactly these 3 nodeids on the box, nothing else (no sweep): `tests/test_vendor_opacity.py::test_local_write_surface_probes[POST-/knowledge/gaps/{gap_id}/answer]`, `tests/test_retention.py::test_sweep_with_explicit_keep_forever_touches_nothing`, `tests/test_reconcile_intent_lookup.py::test_a_gap_answers_intent_does_not_carry_the_reply_key`. Worktree `voicepod-lane-circleci-three-reds` @ trunk `b9fa96a1`, fresh `platform/.venv` via `uv sync --group dev` (built, DEPS-OK).
2. Scope check: PASS — all 3 files under `platform/tests/**` (in-scope glob); report/logs under `audit/**` (always allowed). No product edits until classification; any fix gets its own commit per test.
3. Pre-flight: `git status` clean (verified); `git log -1` = `b9fa96a1`; box via atomic `mkdir` + immediate owner write, census `count: 0` AFTER acquire (release if not), release immediately after the ~1-min run (no hold across edit cycles).
4. Green bar: each test recorded pass/fail with FULL assertion text; STOP conditions: (i) any red I cannot classify as (a)/(b)/(c) with file:line evidence → QUESTION, no fix; (ii) a fix touching a multi-step write (retention sweep) → INTERRUPT-TEST analysis before committing; (iii) a fix needing files outside `platform/**` → QUESTION (merge gate would refuse).
5. First red: classify BEFORE touching — (a) product defect only if it fails locally on trunk for a product reason; (b) environment only with the named missing provider/env var + CI-vs-test fix decision; (c) drift only with the merged shape-change commit identified. Break-test per fix where a predicate exists; report table test → local result → class → commit/why-not. Never merge; end REPORT READY or QUESTION.
