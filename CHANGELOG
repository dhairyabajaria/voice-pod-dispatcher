2026-09-08 06:09  tests/test_clearhold.py 61ade578   [WORKER-2, own scope]
  THE CLASS GUARD WAS DEFINED OVER ONE FILE INSTEAD OF OVER ITS POPULATION. Yesterday's embedded-
  Python checker scanned dispatcherctl.sh alone — green for exactly one path. The day anyone adds a
  `python3 -c "…"` to watch.sh, safe_apply.sh or run_all.sh, the defect the checker exists to catch
  ships again against a fully green suite. Same shape as every discovery-by-literal-name finding in
  this programme, committed inside the guard written to stop it.
  It now enumerates every .sh in dispatcher/ and dispatcher/tests/ and reports per-file counts, so
  an empty scan cannot pass as a clean one.
  MEASURED, not argued: the exact defect class planted in safe_apply.sh — a script the old guard
  never read — is CAUGHT by the widened guard and MISSED by the old one (simulated by restricting
  the same scan to dispatcherctl.sh). safe_apply.sh restored to pre-image md5 e2c53d0d, suite green
  after restore.
  Population today is 2 of 2 in dispatcherctl.sh and 0 elsewhere, which is why this cost nothing now
  and would have been worth nothing later.
  PROOF: tests/test_clearhold.py 15 checks (was 14). Whole suite ALL GREEN.

