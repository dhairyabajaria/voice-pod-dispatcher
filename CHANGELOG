2026-09-10 22:09  dispatcher.py 04061983, tests/test_degradedanswer.py   [BOSS item 7, 2026-09-10 22:12]
  THE SHAPE, NOT THE INSTANCE. The degraded early return is DEFINED by what it protects (opencode)
  and SCOPED by position (everything after it). Those are not the same set, and the difference is
  where things get dropped in silence — item 6 was one instance of it, found by BOSS the hard way.
  MOVED into the degraded pass, own try/except EACH (three functions behind one guard is one
  function's worth of protection), under the queue lock already held, in the healthy path's order:
    * apply_gate_requests — BOSS's hand-named gates. No `http(`, no roster, no session id: a merge
      gate is a subprocess. `dispatcherctl.sh gate` has been silently doing nothing for 30 h.
    * collect_gates — launching a gate nobody collects is worse than not launching it; the findings
      never reach anyone. Guarded by the same auto_gate/manual_gates_live condition as the healthy
      path, not a looser one.
  STILL STRANDED, AND CORRECTLY — stated rather than left as the residue of what nobody moved:
  check_dead, check_stall, check_session_idle, feed, feed_when_idle, post_prompt, roster_refresh,
  recover_missed_reports, prune_offroster, apply_clear_requests. Every one reads the roster, a
  session or the server, so a degraded tick genuinely cannot do them.
  THE THIRD FUNCTION BOSS NAMED IS **NOT** MOVED, AND THE REFUSAL IS THE POINT.
  `clear_feed_holds_for_answered` REMOVES `hold/<EXEC>.feed`. At 22:12 the same message ordered both
  slot holds KEPT after the restart — the owner has stopped all Codex/Muse work and
  `hold/CODEX-1.feed` (21:52:58) is what enforces it. Moving that function would build an AUTOMATIC
  path to deleting the hold the owner's stop depends on: the moment any queued row carries
  `answered_at` with `executor=CODEX-1` — exactly the workflow BOSS is using to answer parked rows —
  the next degraded tick would clear it. Measured: no queued row carries `answered_at` right now, so
  it would not have fired today; the hazard is latent, not theoretical, and the healthy path (which
  has the same behaviour) has not run for 30 h. HELD PENDING HIS RULING. A test asserts its ABSENCE
  so this cannot be re-added silently.
  PROOF: tests/test_degradedanswer.py 17 -> 26 checks. PC a hand-named gate LAUNCHES on a degraded
  tick and its request file is consumed; NC a request for a non-gateable row is refused there exactly
  as on the healthy path; PC a finished gate is COLLECTED; a RAISING gate consumer cancels neither
  the answer consumer, nor collection, nor codex dispatch; source order on the same `_before` helper
  (a missing term is a clean False, never a ValueError).
  MUTATION MEASURED: removing both item-7 calls -> 18 passed, 8 failed, zero tracebacks.
  NOT IN EFFECT UNTIL A RESTART. Not restarted; BOSS holds the word and a codex run is still live.

